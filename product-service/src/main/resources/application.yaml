server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      show-components: always
  health:
    db:
      enabled: true
    diskspace:
      enabled: true
  info:
    env:
      enabled: true

spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mysql://localhost:3307/product_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: techuser
    password: techpass
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
    open-in-view: false

  sql:
    init:
      mode: never

logging:
  level:
    org.hibernate.engine.jdbc.spi.SqlExceptionHelper: error
    org.hibernate.engine.jdbc.env.internal: error
    org.hibernate.orm.deprecation: error
    org.springframework: warn
    com.jdvergara.techtest: info
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

info:
  app:
    name: Product Service
    description: Microservicio de gestión de productos
    version: 1.0.0
    port: 8081
